# Knowledge Base

## Requirements

```bash
brew install pandoc
```

## Get started

Just run:

```bash
bin/build
```

file structure:

* `docs`: public html files
* `md`: markdown source files
* `src`: templates
* `bin`: build tool
